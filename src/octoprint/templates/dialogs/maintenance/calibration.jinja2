<button id="reset-calibration" title="Restart Operation" class="btn btn-default pull-right hidden"
    data-bind="click: finishCalibration">
    <i class="icon-refresh"></i> {{ _('Restart Operation')}}
</button>
<div id="calibrationStep0">
    <div class="row-fluid">
        <div class="span6">
            <strong>{{ _('Step 1')}}: {{ _('Calibration - Table Height') }}</strong>
        </div>
    </div>
    <div class="row-fluid">
        <div class="span2"></div>
        <div class="span8">
            <img src="{{ url_for('static', filename='img/maintenance/calibracao.png') }}" />
        </div>
        <div class="span2"></div>
    </div>
    <br />
    <div class="row-fluid">
        <p>{{ _('Make sure the extruder nozzle is clean before this operation.' ) }}</p>
        <p>{{ _('Put a piece of paper on the table beneath the nozzle. Fine tune the table up or down.' ) }}</p>
        <p>{{ _('You have correctly calibrated the table once the paper is neither stuck nor completely loose.' ) }}</p>
    </div>
    <br />
</div>

<div id="calibrationStep1">
    <div class="row-fluid">
        <div class="span6">
            <button id="start-calibration-btn" title="Start Operation" class="btn btn-primary"
               data-bind="click: startCalibration, enable: printerState.isOperational() && !operationLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-cog"></i> {{ _('Start Calibration')}}</button>
        </div>
    </div>
    <br />
</div>

<div id="calibrationStep2" class="hidden">
    <strong>{{ _('Table Fine Tuning')}}</strong>
    <br /><br />
    <div class="row-fluid">
        <div class="span6">
            <button id="upbigstep-btn" title="Up 0.05" class="btn btn-primary"
               data-bind="click: upBigStep, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-arrow-up"></i> {{ _('0.5 millimeters')}}</button>
        </div>
        <div class="span6">
            <button id="downbigstep-btn" title="Up 0.05" class="btn btn-primary"
               data-bind="click: downBigStep, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-arrow-down"></i> {{ _('0.5 millimeters')}}</button>
        </div>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span6">
            <button id="upsmallstep-btn" title="Up 0.05" class="btn btn-primary"
               data-bind="click: upSmallStep, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-chevron-up"></i> {{ _('0.05 millimeters')}}</button>
        </div>
        <div class="span6">
            <button id="downsmallstep-btn" title="Down 0.05" class="btn btn-primary"
               data-bind="click: downSmallStep, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-chevron-down"></i> {{ _('0.05 millimeters')}}</button>
        </div>
    </div>
    <br />
    <div class="row-fluid">
        <span class="span12 alert alert-info"><i class="icon-exclamation-sign"></i>
            {{ _('WARNING: If you have moved the table up too far against the nozzle, click "Restart Operation".') }}
        </span>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span6">
            <button id="calib-next-step-3" title="Next Step" class="btn btn-default"
               data-bind="click: nextStepCalibration2, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-arrow-right"></i> {{ _('Next Step')}}</button>
        </div>
    </div>
</div>

<div id="calibrationStep3" class="hidden">
    <div class="row-fluid">
        <div class="span6">
            <strong>{{ _('Step 2')}}: {{ _('Calibration - Table Level') }}</strong>
        </div>
    </div>
    <div class="row-fluid">
        <div class="span2"></div>
        <div class="span8">
            <img src="{{ url_for('static', filename='img/maintenance/cabeca_impressao1.png') }}" />
        </div>
        <div class="span2"></div>
    </div>
    <br />
    <div class="row-fluid">
        <span>{{ _('Put a piece of paper on the table beneath the nozzle.' ) }}</span>
        <span>{{ _('Fine tune the table up or down by rotating the left screw.' ) }}</span>
        <span>{{ _('You have correctly calibrated the table once the paper is neither stuck nor completely loose.' ) }}</span>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span6">
            <button id="calib-next-step-4" title="Next Step" class="btn btn-default"
               data-bind="click: nextStepCalibration3, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-arrow-right"></i> {{ _('Next Step')}}</button>
        </div>
    </div>
</div>


<div id="calibrationStep4" class="hidden">
    <div class="row-fluid">
        <div class="span6">
            <strong>{{ _('Step 3')}}: {{ _('Calibration - Table Level') }}</strong>
        </div>
    </div>
    <div class="row-fluid">
        <div class="span2"></div>
        <div class="span8">
            <img src="{{ url_for('static', filename='img/maintenance/cabeca_impressao2.png') }}" />
        </div>
        <div class="span2"></div>
    </div>
    <br />
    <div class="row-fluid">
        <span>{{ _('Put a piece of paper on the table beneath the nozzle.' ) }}</span>
        <span>{{ _('Fine tune the table up or down by rotating the right screw.' ) }}</span>
        <span>{{ _('You have correctly calibrated the table once the paper is neither stuck nor completely loose.' ) }}</span>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span6">
            <button id="calib-next-step-5" title="Next Step" class="btn btn-default"
               data-bind="click: nextStepCalibration4, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-arrow-right"></i> {{ _('Next Step')}}
            </button>
        </div>
    </div>
</div>

<div id="calibrationStep5" class="hidden">
    <div class="row-fluid">
        <div class="span6">
            <strong>{{ _('Final Step')}}</strong>
        </div>
    </div>
    <div class="row-fluid">
        <div class="span2"></div>
        <div class="span8">
            <img src="{{ url_for('static', filename='img/maintenance/etapa_final.png') }}" />
        </div>
        <div class="span2"></div>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span4"></div>
        <div class="span4">
            <button id="test-calibration-btn" title="Calibration Test" class="btn btn-primary"
               data-bind="click: calibrationTestStep1, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-cog"></i> {{ _('Calibration test')}}</button>
        </div>
        <div class="span4"></div>
    </div>
    <br />
    <div class="row-fluid">
        <span>{{ _('Click "Calibration test" if you want to make sure the table has been correctly calibrated.' ) }}</span>
        <span>{{ _('The extruder nozzle will move along the table.' ) }}</span>
    </div>
    <br />
    <div class="row-fluid">
        <p>{{ _('If you want to finish the calibration procedure without testing you can click "Finish Calibration".' ) }}
        </p>
    </div>
    <div class="row-fluid">
        <div class="span6">
            <button id="calib-finish" title="Finish Calibration" class="btn btn-success "
               data-bind="click: finishCalibration, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-ok-sign"></i> {{ _('Finish Calibration')}}
            </button>
        </div>
    </div>
</div>

<div id="calibrationTest1" class="hidden">
    <div class="row-fluid">
        <div class="span6">
            <strong>{{ _('Calibration Test')}}</strong>
        </div>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span4"></div>
        <div class="span4">
            <img src="{{ url_for('static', filename='img/maintenance/bico_extrusao.png') }}" />
        </div>
        <div class="span4"></div>
    </div>
    <br /><br />
    <div class="row-fluid">
        <div class="span4"></div>
        <div class="span4">
            <span><strong>{{ _('PROCESSING...PLEASE WAIT')}} <i class="icon-spinner icon-spin"></i></strong></span>
        </div>
        <div class="span4"></div>
    </div>
    <div class="row-fluid">
        <div class="span12">
            <p>{{ _('The printer is heating up and preparing to run the calibration test. This process may take a few moments.')}}</p>
        </div>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span4"></div>
        <div class="span4">
            <button id="cancel-calib-test" title="Cancel Calibration Test" class="btn btn-default"
               data-bind="click: cancelCalibrationTest, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && loginState.isUser()">
                <i class="icon-ok-sign"></i> {{ _('Cancel Test')}}
            </button>
        </div>
        <div class="span4"></div>
    </div>
</div>

<div id="calibrationTest2" class="hidden">
    <div class="row-fluid">
        <div class="span6">
            <strong>{{ _('Validate Calibration')}}</strong>
        </div>
    </div>
    <br />
    <div class="row-fluid">
        <div class="span1"></div>
        <div class="span4" style="text-align: center;">
            <img src="{{ url_for('static', filename='img/maintenance/validation1.png') }}" />
            <button id="calib-success" title="Finish Calibration" class="btn btn-success "
               data-bind="click: finishCalibration, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-ok-sign"></i> {{ _('Calibration successful')}}
            </button>
        </div>
        <div class="span1"></div>
        <div class="span1"></div>
        <div class="span4" style="text-align: center;">
            <img src="{{ url_for('static', filename='img/maintenance/validation2.png') }}" />
            <button id="calib-repeat" title="Repeat Calibration" class="btn btn-primary "
               data-bind="click: repeatCalibration, enable: printerState.isOperational() && !commandLock()
               && printerState.isReady() && !printerState.isPrinting() && loginState.isUser()">
                <i class="icon-refresh"></i> {{ _('Repeat calibration')}}
            </button>
        </div>
        <div class="span1"></div>
    </div>
    <br />
    <div class="row-fluid">
        <span>{{ _('Remove the table and validate your results.' ) }}</span>
        <span>{{ _('Compare the resulting pattern on the table to the pictures shown above.' ) }}</span>
        <span>{{ _('Choose the option corresponding to the one that looks most like your result.' ) }}</span>
        <span>{{ _('Before proceeding with either one, you must clean both the table and the extruder nozzle placing back the table at the end.' ) }}</span>
    </div>
</div>